version: "2"
run:
  timeout: 5m
  modules-download-mode: readonly
linters:
  default: standard
formatters:
  enable: [gofumpt]
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
output:
  formats:
    text:
      path: /dev/null
    json:
      path: stdout
